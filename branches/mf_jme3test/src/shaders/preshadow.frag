varying float outDepth;

void main(){
   gl_FragColor = vec4(1.0);
   //gl_FragDepth = outDepth;
   //gl_FragDepth = outDepth;
   //gl_FragDepth = gl_FragCoord.z;
   //gl_FragDepth = (gl_FragCoord.z * gl_FragCoord.w) / gl_DepthRange.far;
}