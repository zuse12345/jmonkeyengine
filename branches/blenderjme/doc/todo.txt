$Id$

TODO LIST

Find out how to retrieve export or data file preference directory from Blender.
Default export directory to that.

Enhance script to honor a BLENDER_SWITCHES env variable + to add:
-b /dev/null

Make a blenderexp test that requires the shell script to run, setting
 COORDS if necessary, and adding -noaudio, -nojoystick, -noglsl.

Add silent-overwrite button.
